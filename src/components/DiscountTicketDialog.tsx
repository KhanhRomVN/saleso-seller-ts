import React, { useState, useEffect } from "react";
import axios from "axios";

const DiscountTicketDialog: React.FC<{ discount_id: string }> = ({
  discount_id,
}) => {
  return ();
};

export default DiscountTicketDialog;
